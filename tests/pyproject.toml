[project]
name = "laravel-docker-base-tests"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = ["testshed"]

[tool.pytest.ini_options]
addopts = "--shed --shed-image=laravel-docker-base --shed-image-policy=build --shed-build-context=.. --shed-stubs-dir=stubs --shed-container-logs"
